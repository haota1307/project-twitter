import React from 'react'
import PostItem from '../PostItem'

export default function PostFeed() {
  return (
    <>
      {/* {posts.map((post: Record<string, any>) => (
        <PostItem userId={''} key={post.id} data={post} />
      ))} */}
    </>
  )
}
